# Direct dependencies only — mirrors [project.dependencies] in pyproject.toml.
# Transitive dependencies (altair, boto3, grpcio, etc.) are resolved by pip
# automatically and intentionally not pinned here to avoid drift.
anthropic==0.105.2
pandas==3.0.3
pydantic==2.13.4
typer==0.26.4
rich==15.0.0
python-dotenv==1.2.2
streamlit==1.58.0
pypdf==6.12.2
databricks-sdk==0.112.0
snowflake-connector-python==4.6.0
psycopg2-binary==2.9.12
mysql-connector-python==9.7.0
google-cloud-bigquery==3.41.0
duckdb==1.5.3
fpdf2==2.7.9

# Dev / test
pytest==9.0.3
